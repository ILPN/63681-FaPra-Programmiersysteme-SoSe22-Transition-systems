import { RandomGraphLayout } from './random-graph-layout';

describe('RandomGraphLayout', () => {
  it('should create an instance', () => {
    expect(new RandomGraphLayout()).toBeTruthy();
  });
});
